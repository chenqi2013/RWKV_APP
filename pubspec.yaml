name: zone
description: "An app to show RWKV model in mobile app. Developed with Flutter."
publish_to: "none"
version: 4.2.7+699

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  rwkv_mobile_flutter:
    path: ../rwkv_mobile_flutter
  #    git:
  #      url: https://github.com/MollySophia/rwkv_mobile_flutter
  #      ref: dev

  photo_viewer:
    # Local development, fix some bugs
    # path: ../photo_viewer
    # Git development, for your company's project
    git:
      url: https://github.com/HaloWang/photo_viewer
      ref: main
    # Original repository
    # git:
    #   url: https://github.com/kumamotone/photo_viewer
    #   ref: main

  vclibs:
    # Local development, fix some bugs
    # path: ../vclibs
    # Git development, for your company's project
    git:
      url: https://github.com/HaloWang/vclibs
      ref: main
    # Original repository
    # git:
    #   url: https://github.com/leanflutter/vclibs
    #   ref: main

  gpt_markdown:
    # Local development, fix some bugs
    # path: ../gpt_markdown
    # Git development, for your company's project
    git:
      url: https://github.com/HaloWang/gpt_markdown
      ref: main
    # Original repository
    # git:
    #   url: https://github.com/Infinitix-LLC/gpt_markdown
    #   ref: main

  syntax_highlight:
    # Local development, fix some bugs
    # path: ../syntax_highlight
    # Git development, for your company's project
    git:
      url: https://github.com/HaloWang/syntax_highlight
      ref: main
    # Original repository
    # git:
    #   url: https://github.com/serverpod/syntax_highlight
    #   ref: main

  rwkv_downloader:
    # Local development, fix some bugs
    # path: ../rwkv_downloader
    git:
      url: https://github.com/RWKV-APP/rwkv_downloader.git
      ref: master

  flutter_roleplay:
    git:
      url: https://github.com/chenqi2013/flutter_roleplay
      ref: ee0ba83637085448569aad8c2d0e24532169b0ea

  disable_battery_optimization:
    git:
      url: https://github.com/pvsvamsi/Disable-Battery-Optimizations
      ref: 3bd744d

  adaptive_dialog: 2.5.0
  archive:
  audioplayers:
  background_downloader:
  cached_network_image: ^3.4.1
  collection:
  crypto:
  cupertino_icons:
  device_info_plus:
  dio: ^5.9.0
  drift:
  drift_flutter:
  equatable:
  file_picker:
  flutter_dotenv:
  flutter_native_splash:
  flutter_riverpod: 2.6.1
  flutter_staggered_grid_view: ^0.7.0
  flutter_svg: ^2.2.3
  font_awesome_flutter: 10.12.0
  freezed_annotation:
  gaimon:
  gal:
  go_router:
  google_mlkit_text_recognition:
  halo: ^1.0.0
  halo_alert: ^1.0.0
  halo_state: ^1.0.0
  http:
  image_picker:
  intl:
  json_annotation:
  macos_secure_bookmarks: ^0.2.1
  material_symbols_icons:
  mp_audio_stream:
  package_info_plus:
  path:
  path_provider:
  permission_handler:
  pretty_qr_code:
  record:
  rxdart:
  sentry_flutter:
  share_plus:
  shared_preferences:
  shelf:
  shelf_web_socket:
  sprintf:
  sqflite_common_ffi:
  system_info2:
  url_launcher:
  wakelock_plus:
  web_socket_channel:

dependency_overrides:
  rwkv_mobile_flutter:
    path: ../rwkv_mobile_flutter

dev_dependencies:
  build_runner:
  custom_lint:
  drift_dev:
  flutter_test:
    sdk: flutter
  flutter_gen_runner:
  flutter_lints:
  freezed:
  icons_launcher:
  import_sorter:
  json_serializable:
  sentry_dart_plugin:
  test:

flutter:
  uses-material-design: true

  assets:
    - assets/lib/chat/
    - assets/lib/font_preview/
    - assets/model/chat/
    - assets/config/chat/
    - assets/img/chat/
    - remote/latest.json
    - .env
    - assets/filter.txt
    - assets/lambada_test.jsonl
    - assets/config/code_highlights/

    # QNN libraries for Android
    - path: assets/lib/qnn/
      platforms: [android]

    # QNN libraries for Windows (arm64), uncomment when running on Windows ARM64
    # - path: assets/lib/qnn-windows/
    #   platforms: [windows]

flutter_intl:
  enabled: true # Required. Must be set to true to activate the plugin. Default: false
  class_name: S # Optional. Sets the name for the generated localization class. Default: S
  main_locale: zh_Hans # Optional. Sets the main locale used for generating localization files. Provided value should consist of language code and optional script and country codes separated with underscore (e.g. 'en', 'en_GB', 'zh_Hans', 'zh_Hans_CN'). Default: en
  arb_dir: lib/l10n # Optional. Sets the directory of your ARB resource files. Provided value should be a valid path on your system. Default: lib/l10n
  output_dir: lib/gen # Optional. Sets the directory of generated localization files. Provided value should be a valid path on your system. Default: lib/generated
  use_deferred_loading: false # Optional. Must be set to true to generate localization code that is loaded with deferred loading. Default: false

sentry:
  upload_debug_symbols: true
  upload_source_maps: true
  project: rwkv0
  org: ce-wang
