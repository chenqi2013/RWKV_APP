---
description: Flutter UI — no _build helpers, MediaQuery.sizeOf/paddingOf, Color.q
globs: "lib/**/*.dart"
alwaysApply: false
---

## 禁止使用 Divider 组件, 必须使用 Container 组件来实现

所有你想要使用 `Divider` 组件的时候, 必须使用 `Container` 组件来实现, 并且设置 `height` 为 0.5, 选择合理的颜色(首选 `final qb = ref.watch(P.app.qb);`)

## 禁止使用 ListTile 组件, 必须使用原始的 Column + Row 来实现 List 条目

## 禁止在 UI 层实现任何多于三行的逻辑代码

严禁在 /page, /widget, 目录下实现任何多于三行的逻辑代码, 尤其像是设置 state provider 属性, 加载权重

所有的这些较重的逻辑方法都要放到 /store 目录下对应的文件中去实现

/page, /widget 的职责是构建 UI, 把 state provider 和 provider 绑定到 widget, 不应该实现任何 onclick 之类的方法

### 严禁你实现的方法带有类似的东西

```dart
void Function()? onSuccess,
void Function(Object)? onError,
```

直接在实现里使用 `Alert.success` 和 `Alert.error` / `Alert.warning` 来显示成功和错误信息

## 声明 Theme

必须要在 `Widget build(BuildContext context, WidgetRef ref) {` 方法的顶部就声明 `final theme = Theme.of(context);`. 严禁在后继的代码中使用 `Theme.of(context)` 来获取主题

## 禁止使用任何 `FutureBuilder` 方法

严格禁止在 build 方法中使用 future builder

## 不要在 UI 层的 class 中声明任何 `Widget _build` helper 方法

Flutter 官方视频说: [使用 `Widget _build` 形式的 helper 方法会降低程序的渲染性能](https://youtu.be/IOyq-eTRhvo?si=aBXuUVXIkirPqQVR&t=99)

Bad 😡

```dart
class WidgetName extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return _buildFooBar(context, ref);
  }

  Widget _buildFooBar(BuildContext context, WidgetRef ref) {
    return const SizedBox();
  }
}
```

Good ✅

```dart
class WidgetName extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return _SubWidgetName();
  }
}

class _SubWidgetName extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return const SizedBox();
  }
}
```

## 使用 `MediaQuery.sizeOf(context).width` 来获取屏幕宽度

- 使用 `MediaQuery.sizeOf(context).width` 来获取屏幕宽度, 不要使用 `MediaQuery.of(context).size.width`, 因为 `MediaQuery.of(context).size.width` 会触发 `MediaQuery.of(context)` 会监听各种 MediaQuery 的变化, 导致非必要的重回, 比如键盘展示 / 收起事件

```dart
Widget build(BuildContext context, WidgetRef ref) {
  final width0 = MediaQuery.sizeOf(context).width; // ✅ Good
  final width1 = MediaQuery.of(context).size.width; // 😡 Bad
  return SizedBox(width: width0, height: 100);
}
```

## 使用 `MediaQuery.paddingOf(context)` 来获取屏幕内边距

- 使用 `MediaQuery.paddingOf(context)` 来获取屏幕内边距, 不要使用 `MediaQuery.of(context).padding`, 因为 `MediaQuery.of(context).padding` 会触发 `MediaQuery.of(context)` 会监听各种 MediaQuery 的变化, 导致非必要的重回, 比如键盘展示 / 收起事件

## 使用 `.q(.x)` 来修改 `Color` 的透明度

```dart
Color color = Colors.red;
color = color.withOpacity(0.5); // Bad 😡
color = color.q(.5); // Good ✅
```

## 忽视项目中的 albatross 逻辑

仅保证编译通过即可

## 忽视项目中的 flutter_roleplay 逻辑

仅保证编译通过即可
